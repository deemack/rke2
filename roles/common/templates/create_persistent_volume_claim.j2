apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "{{ name }}"
spec:
  storageClassName: manual
  accessModes:
    - "{{ accessModes }}"
  resources:
    requests:
      storage: "{{ storage }}"