apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "{{ name }}"
spec:
  storageClassName: manual
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: "{{ storage }}"