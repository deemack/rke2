apiVersion: v1
kind: PersistentVolume
metadata:
  name: "{{ name }}"
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: "{{ capacity }}"
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "{{ path }}"